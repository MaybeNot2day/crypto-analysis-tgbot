# Environment variables for Crypto Outlier Detection Dashboard
# Copy this file to .env and fill in your actual values

# Exchange API Keys (optional for public endpoints, but recommended for higher rate limits)
# For Binance: Get your API keys from https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=
BINANCE_API_SECRET=

# CoinGecko API Key (optional, free tier allows limited requests)
# Get from https://www.coingecko.com/api/pricing
COINGECKO_API_KEY=

# Database Credentials (if using TimescaleDB instead of DuckDB)
DB_USER=
DB_PASSWORD=

# Configuration Path (optional, defaults to config/config.yaml)
CONFIG_PATH=config/config.yaml
